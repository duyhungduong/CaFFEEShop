const stripe = require("../../config/stripe_usd");

const endpointSecret = process.env.STRIPE_ENPOINT_WEBHOOK_SECRET_KEY

const webhooks = (request, response) =>{
    try {
        
    } catch (error) {
        
    }
}